#ifndef DISTANCE_H
#define DISTANCE_H

#include "Point.h"
#include "PointRegistry.h"
#include <GeographicLib/Geodesic.hpp>

class Distance {
public:
  virtual ~Distance() {}
  virtual double calculate(const Point& p1, const Point& p2);
};

class GreatCircleDistance : public Distance {
public:
    // Only the declaration here!
  double calculate(const Point& p1, const Point& p2);
};

class EuclideanDistance : public Distance {
public:
  double calculate(const Point& p1, const Point& p2);
};

#endif // This closes the first #ifndef

